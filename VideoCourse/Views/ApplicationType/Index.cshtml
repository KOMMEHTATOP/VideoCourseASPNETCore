@model IEnumerable<VideoCourse.Models.ApplicationType>

    <div class="container p-3">
    <div class="row" pt-4>
            <div class="col-6">
                <h2 class="text-primary">ApplicationType List</h2>
            </div>
            <div class="col-6 text-end">
                <a asp-action="Create" class="btn btn-primary">Create New Application Type</a>
            </div>
        </div>


        <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td width="50%">@item.Name</td>
                        <td>
                            @* <a href="@Url.Action("Edit", "Category", new { id = item.Id })" class="btn btn-primary">Edit</a> *@
                            @* <a href="@Url.Action("Delete", "Category", new { id = item.Id })" class="btn btn-danger">Delete</a> *@
                        </td>
                    </tr> 
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-warning">No application type Found</div>
    }

    </div>

